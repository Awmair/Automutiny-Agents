# Deterministic seed

`pnpm seed` replaces only the configured firm's fixed seed records. Set `FIRM_NAME`; visitor sessions and their records are preserved.

All IDs, dates and values are fixed so every reset produces the same study environment.
