# Shared database

Server-only Supabase client and typed reads shared by the website and all agents.

Agent-specific rules do not belong here. They stay in the matching folder under `packages/agents`.
